var friendsName = ["manal", "anfaal", "rohail", "romail"];
// we need one name at a time so we use foreach function
friendsName.forEach(function (onefriendName) { return console.log(onefriendName); });
